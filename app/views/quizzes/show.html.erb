<%= @quiz.title %><br>
<%= @quiz.description %><br>
<br>
<br>

<%= form_for(@quiz, :url => course_quizzes_path(@course.id), :method => 'post') do |f| %>
  <%= f.label :Submitted %>
  <%= f.check_box :status %>
  <%= f.submit "Mark as submitted" %>
<% end %>






<%= link_to 'Go to list of quizzes for this class', course_quizzes_path(@quiz.course_id) %>
